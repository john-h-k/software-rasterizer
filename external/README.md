# This is where external libraries (windows for now) will be stored and searched for by CMake

You must:
 - Download GLFW, place the include file in `./include/`
 - Place the `vc19` dlls + libs in `./libs`
